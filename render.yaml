services:
  - type: web
    name: glow-fix-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: cd server && npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: NODE_ENV
        value: production
