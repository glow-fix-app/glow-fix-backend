{
  "name": "@glow-fix/types",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo",
    "lint": "eslint src/**/*.ts",
    "dev": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}