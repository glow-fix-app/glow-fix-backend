{
  "name": "@glow-fix/config",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "tsc"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "typescript": "^5.0.0"
  }
}
